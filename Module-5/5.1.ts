//  mongodd command line command
/*  1.show dbs => to show data base
2. to create new database => use databaseName
3. to create collection => db.createCollection("collection Name")
4. to add json data in a collection => db.getCollection("collection Name").insertOne({
    json data
})
5. to see find the data => db.getCollection("collection name").find()

  */
